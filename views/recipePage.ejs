<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Recipes</title>
</head>
<body>
  <h4> You are viewing this page as an administrator </h4>
  <%- include ('./partials/navbar') %>
    <h1>All Recipes!</h1>
    <% for(let i = 0; i < recipes.length; i++) { %> 
        <div>
          <h2><%= recipes[i].title %></h2> 
          <p>By: <%= recipes[i].author.name %></p> 
          <p>Ingredients: 
            <% recipes[i].ingredients.forEach(ingredient => { %>
              <div><%= ingredient.name %>: <%= ingredient.quantity %></div>
            <% }) %> 
          </p> 
          <p><%= recipes[i].instructions %></p>
        </div>
        <form action="/recipe/<%= recipes[i].recipe_id %>?_method=DELETE" method="POST" onsubmit="return confirm('Are you sure you want to delete this recipe?');">
          <button type="submit">Delete Recipe</button>
        </form>
      <% } %>
</body>
</html>